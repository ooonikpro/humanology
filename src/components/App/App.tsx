import React from 'react'

import { AppHeader } from "../AppHeader";
import { AppContent } from "../AppContent"

export const App = () => {
  return (
      <>
        <AppHeader />
        <AppContent />
      </>
  );
};